# Trigger production image: server-only scene analysis.  Keep the direct
# detector, OpenCV implementation, and numerical base explicit before compiling
# the fully hashed Linux lock below.
scenedetect-headless==0.7.1
opencv-python-headless==4.12.0.88
numpy==2.2.6
