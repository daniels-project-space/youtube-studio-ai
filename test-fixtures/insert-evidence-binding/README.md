# Reviewed insert handoff evidence

Synthetic reviewed source packets; not real-world factual verification.

`planner-before.json` is the failed real OpenRouter pilot, before ordinary and strict planning instructions were separated. `planner-after.json` is the successful real pilot after that root change. The source context is `context.json`. Neither response was manually corrected. Reported total cost: $0.00941025 across two calls on the existing pinned route.

The test and renderer scripts exercise the actual `visual_inserts` block and production Remotion renderer. External boundaries are controlled where specified. `evidence.json` binds five completed native clips and 45 inspected samples under `media/`, including the linked irregular-axis and recovered stat proofs. A sixth clip uses the actual production finishing function over contrasting backgrounds: 32 seconds, 960 frames, ten inspected samples, and four exact decoded outside-window comparisons. The interrupted stat attempt is not included. This directory is excluded from Vercel application inputs.

See `docs/INSERT_EVIDENCE_BINDING_REVIEW_2026-09.md` for scope, measured failures and open requirements.
