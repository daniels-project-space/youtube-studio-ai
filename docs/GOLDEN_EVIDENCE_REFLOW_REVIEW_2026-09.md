# Golden evidence subpanels — separate follow-up

This follow-up remains **outside frozen card-layout candidate `56be19f`**. It changes presentation only: no qualification policy, proof promotion, preserved media or module lock.

The real-browser baseline opened Evidence/admission and Universal foundation, then every nested native disclosure. It failed six viewport/panel cases (`/tmp/ysa-golden-evidence-baseline.log`, `/tmp/ysa-golden-evidence-iO6cM7/`): an expanded panel occupied only half the available desktop space while its closed neighbor stretched into a huge empty column; at 200% text, foundation stages collided and admission names/reasons were clipped. Desktop evidence and enlarged foundation screenshots were inspected.

The first draft gives an opened assurance panel the complete row, keeps closed neighbors content-height, and sizes nested grids against the panel width. Family names, blocker details and successor IDs wrap; foundation text is larger. No new UI wrapper, provider request, event handler or query was added. Ten expanded panel cases passed with no page errors (`/tmp/ysa-golden-evidence-pass1.log`, `/tmp/ysa-golden-evidence-TqZoN2/`). Those checks do not certify the rest of Golden or the pipelines themselves.

Visual inspection then found descriptions vertically separated from their headings by stretched grid tracks. A stronger audit checks that gap and verifies every foundation title/count against the real engine registry, adding 320 px and enlarged tablet cases. The corrected CSS aligns article content to the start; the earlier draft is being retained as that spacing test's failing baseline before syncing the fix to the local viewer.

Full isolated release tests/build and exact production verification remain required for this follow-up. The earlier card candidate's passing browser checks and ongoing build/tests must not be reused as a completed gate for these additional CSS changes.

The first spacing check measured stretched element boxes and missed the visible gap (`/tmp/ysa-golden-evidence-spacing-baseline.log`). Measuring the actual text ranges correctly rejected the earlier draft in desktop, desktop-200% and tablet cases (`/tmp/ysa-golden-evidence-spacing-baseline2.log`, `/tmp/ysa-golden-evidence-gf995b/`). After aligning text content, the stronger seven-layout audit passed all **14 parent-panel cases**, all nested disclosures, exact engine-registry foundation titles/count and text spacing, with no page errors (`/tmp/ysa-golden-evidence-pass2.log`, `/tmp/ysa-golden-evidence-5gDNtk/`). Desktop-200% foundation and 320 px evidence screenshots were inspected. A separate all-card regression is running against the combined local CSS.

The third visual-validation run also passed all seven layouts with no page errors (`/tmp/ysa-golden-evidence-pass3.log`, `/tmp/ysa-golden-evidence-SS08oo/`). The independent all-card regression on the combined draft passed 280 collapsed/opened pairs, all existing toolbar focus/hit tests and the single shared lock subscription (`/tmp/ysa-golden-evidence-card-regression.log`, `/tmp/ysa-golden-layout-osBlsK/`). Root typecheck and scoped proof-script lint passed. Final local code graph: 21,623 nodes, 52,759 edges, 701 communities, no paid semantic extraction; excluded from application deployment. This still needs its own frozen full release gate and production validation.

## Frozen gate and continuation

Candidate `aa96282a8a004912ab1ed6da88c24a43dcd9acea`, parent card release `56be19f`, is on `checkpoint/golden-evidence-reflow-20260909`. The clean worktree is `/tmp/ysa-golden-evidence-release-VGypcC/repo`. Its direct full-suite log is `/tmp/ysa-golden-evidence-release-tests.log`; typecheck/lint/build logs use the same prefix. The separate audit/defect-proof/assembly chain uses `/tmp/ysa-golden-evidence-release-audits.log`, `/tmp/ysa-golden-evidence-release-defect-proof.log`, `/tmp/ysa-golden-evidence-release-assembly.log`. Typecheck, lint, build, audits, defect proofs and assembly have completed; the direct full suite is still running. Do not push this candidate until all gates finish cleanly, then check provider status, exact alias and both production browser proofs.

The destination audit is a separate read-only follow-up outside this frozen candidate. It found further presentation issues in Studio assets and SEO, but did not test owner access, asset counts or tool execution. Keep those and all older goal items open rather than extending the Golden card completion claim.

## Completed release

The frozen candidate completed **all 631 direct readiness tests with exit 0**, in addition to its already completed typecheck, build, zero-error lint (33 existing warnings), unchanged audits, 24-defect proof and actual 31.021995-second assembly (`/tmp/assembly-smoke-nJAxO7/bk_smoke_2_loudnorm.mp4`). It was fast-forwarded to main only after the full gate finished.

[Vercel deployment succeeded](https://vercel.com/danielmabro-news-projects/youtube-studio-ai/6mGrNb86zAr6r73thR8QJgsQmpjv); the exact production alias returned full `aa96282a8a004912ab1ed6da88c24a43dcd9acea`. Unmodified candidate scripts then passed both production proofs with no endpoint overrides: fourteen parent-panel cases across seven layouts (`/tmp/ysa-golden-evidence-production-aa96282.log`, `/tmp/ysa-golden-evidence-wKYkle/`) and all 280 card pairs (`/tmp/ysa-golden-cards-production-aa96282.log`, `/tmp/ysa-golden-layout-TeWYNN/`). Desktop enlarged-text foundation and 320 px evidence screenshots were inspected. These are UI proof, not pipeline-quality promotion.

[Cloud CI 34352067117](https://github.com/daniels-project-space/youtube-studio-ai/actions/runs/34352067117) completed successfully. Canonical Convex was ready **12:50:00 UTC**; Trigger **20260909.13** deployed **12:51:58 UTC**, worker `worker_cmtu3kym15tui0pn44x7moa1j`, unchanged backend content hash `101fa3a5b4b7ad6781cca1df216d69e9` (`/tmp/ysa-golden-evidence-cloud-deploy.log`). Studio-assets work remains a separate later slice.
