# Lo-Fi reference page — composition-first layout

## Baseline and actual wiring

The previous `/lofi` page displayed an admitted still image beneath a large header/status rail, cropped it into a near-square phone panel and overlaid its caption. A decorative route diagram and 331–334 initially visible words obscured the useful reference. It also nested a second `main` inside AppShell and called the destination “Music references” although it contains no music player.

Graphify was queried before focused review of the page, stylesheet, actual Golden reference adapter, manifest, route labels and callers; Serena was unavailable. Installed Next server/client-component guidance and owner-lock markers were checked. The existing server component now calls the same `goldenProofMediaPresentation` adapter as Golden modules instead of duplicating a hardcoded media path. Historical media remains inadmissible.

The public image is unchanged: `lofi-beachcafe-image`, status `reference`, 1280×714, SHA-256 `5b993ad2fa7dd6b55aec240f17b649d2147447dd81aa00dd01aca38bbb4f3dbb`. No generation prompt, channel identity, runtime admission, manifest, paid model or worker contract was changed. The historical meadow video remains excluded. This is a reference-page improvement, not qualification of the music/loop pipeline or a thumbnail backfill.

## Three distinct visual passes

1. **Hierarchy and real controls:** replaced the decorative map/status rail with the uncropped still, caption below the image, four composition cues, a real full-image link and a return to Golden modules. All six prior release/provenance requirements remain in one initially closed native disclosure. Removed the nested main landmark. The five-layout proof reduced visible copy to 67 words and preserved the exact image response hash; it caught the enlarged-desktop image link becoming covered by fixed navigation after closing the disclosure. `/tmp/ysa-lofi-reference-pass1.log`, `/tmp/ysa-lofi-reference-eCRjeQ/`.
2. **Keyboard/focus repair:** added scoped scroll margins to the actual links and disclosure, then exercised native keyboard opening/closing, source navigation and history return again. All five layouts passed. Changed the focus cue to describe the small figures rather than implying one tightly specified hero. `/tmp/ysa-lofi-reference-pass2.log`.
3. **Density and legibility:** made the four cues an adaptive two-column layout where space permits, retaining a single column for narrow/enlarged text. Increased cue text size, checked caption position, full control hit targets and element clipping, and inspected phone, enlarged-desktop focus and expanded-requirement screenshots. All five layouts passed. `/tmp/ysa-lofi-reference-pass3.log`, `/tmp/ysa-lofi-reference-suEKV3/`.

The final pass additionally makes the top-bar and Golden-card labels consistently “Lo-fi references” and clicks the full actual Golden → reference → Golden → reference round trip. Final local proof: `/tmp/ysa-lofi-reference-final.log`, `/tmp/ysa-lofi-reference-27qMol/`; all five layouts pass with zero browser errors and zero mutation requests. Desktop and phone final screenshots were inspected.

## Independent oracle and functional coverage

The same browser checks reject **37 checks across all five layouts** on unchanged production at `c58adc6` (`/tmp/ysa-lofi-reference-baseline-final.log`, `/tmp/ysa-lofi-reference-jj1Yap/`). They test actual response bytes and natural image ratio, not just screenshot file existence or a matching URL. The baseline has actual aspect-ratio errors of 0.255–1.014; the revised page is below 0.00008. At ordinary phone size the image starts at 220.5 px instead of 383.6 px. Default copy is 67 words instead of 331–334. These are layout/copy measurements, not audience or billing gains.

Coverage includes 1440 px desktop, 390 px phone, 320 px phone, and desktop/phone with doubled root text size; real image decode/hash; caption below the image; no nested main or horizontal/element overflow; keyboard disclosure open/close; retained release boundaries; focus targets unobscured by fixed chrome; full-image navigation and history return; and the actual catalog route/card controls. Local transport forwards only anonymous read-only public GETs; it does not grant owner access or replace the page with a fixture. Exact production must be checked without that transport override.

The existing archive test now renders the real server component using the actual manifest adapter and Next link. Only Node CSS loading is stubbed; the browser proof checks actual compiled styles. It verifies the original file SHA, reference status, native disclosure, links, unchanged boundaries, historical-video rejection and matching top-bar label. The original identity/catalog assertions remain. The page adds no client component, effect, subscription, dependency, provider call or ornamental animation.

## Release status

Scoped release **`41a4b767600af957639972ff7bb95369789dfb99`**, frozen at `/tmp/ysa-lofi-release-HpRbme/repo`, passed **all 631 direct tests**, typecheck, production build, zero-error lint (33 existing warnings), unchanged structural audits and 24 defect proofs. Actual hermetic assembly passed with a 31.021995-second 1920×1080 output at `/tmp/assembly-smoke-lMkOhc/bk_smoke_2_loudnorm.mp4`. Logs use `/tmp/ysa-lofi-release-` with tests, typecheck, lint, build, audit, defect-proof and assembly suffixes. The frozen browser proof independently passes all five cases (`/tmp/ysa-lofi-frozen-browser.log`, `/tmp/ysa-lofi-reference-2v56M5/`); runtime files were byte-compared against the tested local app.

After an ordinary fast-forward main push, the exact production health endpoint returns the full release revision above. Vercel production deployment: https://vercel.com/danielmabro-news-projects/youtube-studio-ai/9NRs3xm2Sefbhgnh6yVYwgaCvV8i. All five **actual production** reference cases pass with no proxy, browser errors or mutation requests (`/tmp/ysa-lofi-production-41a4b76.log`, `/tmp/ysa-lofi-reference-VJJHtU/`). The unchanged script verifies original image bytes, source/history controls and the full Golden-card round trip. Desktop, phone and enlarged-phone focus screenshots were inspected. Six navigation journeys also pass (`/tmp/ysa-lofi-nav-production-41a4b76.log`, `/tmp/ysa-navigation-reflow-PlmmXi/`) and the eight-case shared-header pixel proof remains clean (`/tmp/ysa-lofi-topbar-production-41a4b76.log`, `/tmp/ysa-topbar-contrast-6Ykn04/`).

Cloud workflow **`34362344177` completed successfully**: canonical Convex ready **14:26:25 UTC**, Trigger **`20260909.16`** deployed **14:28:25 UTC**, worker `worker_cmtu70zh689i50vn3qgah0pmh`, content hash `101fa3a5b4b7ad6781cca1df216d69e9`. Log: `/tmp/ysa-lofi-cloud-deploy.log`. The exact health endpoint was rechecked after cloud completion and still returns the full `41a4b76` revision above. The separately held accounting/title code is not included in this release. Paid routes and all remaining whole-goal requirements stay open. Enlarged-phone screenshots also expose text behind the translucent bottom navigation: the source button remains unobscured, but the shared dock needs its own contrast follow-up rather than treating the top-bar result as proof of every fixed surface.
