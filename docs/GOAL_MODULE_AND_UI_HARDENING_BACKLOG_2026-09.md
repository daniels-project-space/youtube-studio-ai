# Goal phase — module intelligence, efficiency, integration, and Studio refinement

**Added:** 2026-09-08  
**Scope:** every production module except the thumbnail module, plus every Studio page and subpanel  
**Rule:** this is a measured hardening program, not a prompt-polish sweep. No item is complete without a current baseline, an independent oracle that is proven to reject a bad case, real-output evidence, wiring evidence, and isolated plus integrated tests.

## Relationship to the ongoing goal

This is an additive goal ledger, not a replacement plan. Earlier unfinished work remains active, including the [page/subpanel overhaul matrix](STUDIO_UI_OVERHAUL_2026-08.md), [fifth-pass findings](UI_FIFTH_PASS_AUDIT_2026-09.md), and [module hardening method](MODULE_HARDENING_METHOD.md). Section H explicitly preserves decisions that a generic module/UI checklist could otherwise lose. The thumbnail module is excluded from the new module-rewrite sweep, **not** from finishing already-requested thumbnail application, consistency, source fidelity, and quality validation. Historical roadmaps do not override later owner decisions or current locked contracts.

**Owner continuation rule, 9 September:** unavailable provider credits leave that specific paid step pending. Continue another actionable goal item; do not repeatedly retry a credit failure, silently switch quality/provider, or stop the wider goal when unpaid implementation, validation, UI or other work remains.

**Owner throughput rule, 9 September:** plan related changes as meaningful batches. Reuse current evidence for unchanged surfaces, run focused failing-case/functional/visual checks during iteration, and run one full frozen release gate per accepted batch rather than per cosmetic pass. Prepare the next independent work while tests/deployments run. Consolidation must not weaken checks, conflate fixture evidence with live results, mix held paid drafts into production, or hide unfinished work.

### Current evidence, not whole-phase completion

- **Recent saved videos:** [practical cards and native-player recovery](RECENT_RENDERS_LAYOUT_REVIEW_2026-09.md) are live at `9795d2a`, verified Production deployment 6356748545. The frozen release passed all 638 tests, full build/typecheck/lint/audit/assembly gates, 16 native recovery cases and three complete caller cases. Five production layout/playback profiles, 21 navigation surfaces and inspected desktop/phone native15s previews/dialogs pass. Initial and native Retry preserve keyboard focus; repeated opens reuse valid cache receipts. Cloud CI 34389882078 passed the repaired package step and is still running its quality gate. No legacy footage quality or whole-goal completion is inferred.

- **Shared asset URLs:** [the request/playback-lifetime repair](ASSET_URL_CACHE_REVIEW_2026-09.md) shares concurrent signing, bounds the cache, rejects invalidated late responses and preserves mounted media URLs. Release `be0439b` passed all 635 tests and full build/audit/assembly/browser gates, is verified at Production deployment 6356086495, and passes five production overview profiles plus inspected desktop/phone native15s previews. Controlled simultaneous signing falls from six requests to one; no fleet billing claim is made. Both cloud CI attempts failed on an unrelated apt repository checksum before tests/deployment. A narrowly scoped signed-Ubuntu APT repair passes actual-shell regression and isolated real-index/install-simulation checks; cloud validation remains pending. Long-playback recovery is now being qualified in the actual saved-video caller, not claimed for every consumer.

- **Shared preview lifecycle:** [root-cause repair and before/after evidence](MEDIA_PREVIEW_LIFECYCLE_REVIEW_2026-09.md) removes duplicate loading labels, avoids signing unused sources and restores native saved-video fallback after reviewed-image failure. Release `69ad087` passed all 634 tests and full build/audit/assembly/browser gates, is verified on the exact Production alias, and passes all five live overview profiles. Cloud CI `34382724352` succeeded: Convex ready 17:33:52 UTC and Trigger `20260909.22` deployed 17:35:55 UTC. No asset/model/provenance change was made. The live Lo-fi master already reaches 15s correctly; this is not a new video qualification or proof of every consumer.

- **Queued cloud release guard:** [ordering and trigger-edge review](CLOUD_RELEASE_EFFICIENCY_REVIEW_2026-09.md) has real Git/CLI and parsed-workflow tests. It preserves waiting releases, skips stale code before either provider write, and allows a tested code revision behind only verified CI-ignored documentation changes. Release `dd6e176` passed the full frozen gate and cloud CI `34381359899`; the real current-main guard also passed for `69ad087`. All quality/credential gates remain; unchanged-runtime fingerprinting, successful-runtime reuse and measured savings are not yet implemented.

- **Studio overview batch:** [three concrete layout/data passes](STUDIO_OVERVIEW_LAYOUT_REVIEW_2026-09.md) remove redundant panels, expose all issue targets, scope analytics consistently, distinguish uploads from releases and add smaller family-aware channel cards. The page uses six top-level subscriptions instead of seven; existing persisted stage data drives active progress without a new query. Release `dd6e176` passes all 634 tests, actual assembly and full build/typecheck/lint/audit/proof gates. Exact Production deployment 6355280788 and alias are verified; all five production browser profiles pass, including exact plan-card navigation, >=44px controls and inspected screenshots. Desktop/phone heights fall 15%/18% with larger text and full titles. Cloud CI 34381359899 succeeded: canonical Convex 17:22:16 UTC, Trigger `20260909.21` 17:24:18 UTC. This does not complete item 96 or all master controls.

- **Reference-page batch:** [three local Lore/Lo-fi passes](REFERENCE_PAGES_LAYOUT_REVIEW_2026-09.md) clear 40 reproduced old-Lore failures. Exact manifest-approved uncropped imagery, functional source/Golden links and native disclosures replace decorative panels; Lore's visible text falls from 442–445 to 74 words while its full-story rule and release boundaries remain intact. Both five-case browser proofs pass with inspected screenshots, and shared styling removes duplicated/obsolete CSS. Release `2eedd75` passed all 633 direct tests, actual assembly, typecheck/build/lint/audits and ten production browser cases without proxies. Exact Production deployment 6354359134 and alias revision are verified; cloud CI 34376140513 is tracked separately.
- **Reported-charge follow-up:** [the response-cost repair](OPENROUTER_REPORTED_CHARGE_REVIEW_2026-09.md) reproduces 22 failures in the initial 25-case transport regression. The corrected 26-case test and actual engine/held-metadata recovery fixtures preserve discounted, higher, zero and BYOK charges; incomplete BYOK evidence stops the held metadata path before its next purchase. All 66 retained responses replay unchanged ($0.1895565), with zero live requests or historical writes. Isolated release `552f84c` passed all 633 tests, build/typecheck/lint, audits, actual assembly and 30 exact production-format response replays. Exact Vercel Production deployment 6353954304 and six live navigation cases are verified; cloud CI 34373798730 succeeded: Convex ready 16:13:10 UTC, Trigger `20260909.19` deployed 16:15:15 UTC. The held title/lease work and finite paid-admission bound are not enabled by this fix; all 151 requirements remain in scope.
- **Mobile navigation contrast follow-up:** [three local passes](MOBILE_NAVIGATION_CONTRAST_REVIEW_2026-09.md) reproduce eleven old dock/menu failures and clear all 21 actual-page painted-surface comparisons, with six working navigation journeys and inspected screenshots. Gradients and the adaptive dock are retained; no new UI/data layer is added. The rounded-corner oracle correction is documented and still rejects the old implementation. Release `2093d7e` passed all 632 tests, real assembly, build/typecheck/lint/audits, all 21 production surface comparisons and six navigation journeys. Screenshots were inspected. Exact Vercel alias and Cloud CI `34367972866` are verified: Convex ready 15:18:35 UTC, Trigger `20260909.18` deployed 15:20:37 UTC. This does not finish interior page redesigns.
- **Metadata request evidence follow-up:** [current provider-contract research and full-source transport checks](METADATA_REQUEST_ENVELOPE_REVIEW_2026-09.md) show that the 66 retained live calls covered only 457–1,362 input tokens. Two larger source fixtures now traverse all seven permitted real-module/transport calls with 123–139 KB messages and exact immutable recovery. Their pricing is explicitly synthetic, not a safe production bound or new model-quality evaluation. Reuse the existing store-aware envelope API; exact input/rate admission, reported-charge reconciliation and versioned paid activation remain open. No source truncation, model downgrade or paid request was used.

- **Run-media layout:** [the focused redesign](RUN_MEDIA_LAYOUT_REVIEW_2026-09.md) was verified on the exact Vercel production alias at `1ded1d6`. It passed all 630 direct tests, actual hermetic assembly, build/typecheck, unchanged audits and zero-error lint. Production browser checks with no proxy show 130 px captions instead of 576 px, a 28% wider video and 13% shorter desktop media section, preserving current thumbnail/history and one combined media subscription. Real video/audio playback, actual caption readback, keyboard disclosure and screenshots cover desktop/mobile/200% text. Cloud CI `34344663820` completed: Convex ready 11:29:11 UTC, Trigger `20260909.9` deployed 11:31:07 UTC. Broader page quality is not implied.
- **Navigation follow-up:** [the adaptive dock/selector pass](NAVIGATION_REFLOW_REVIEW_2026-09.md) is verified on the exact Vercel alias at `2fcbc31`, after all 631 readiness tests, actual assembly, build/typecheck, zero-error lint and unchanged audits passed. Six actual production browser cases pass with all routes reachable, real channel selection and keyboard focus return. It uses a three-target dock only when space requires it, larger labels, wrapping names and a scrollable dropdown without cryptic template codes. Cloud CI `34346555151` completed: Convex ready 11:48:31 UTC, Trigger `20260909.10` deployed 11:50:26 UTC. Interior page issues are not hidden by this result.
- **Room controls follow-up:** [the room repair](CHANNEL_ROOM_LAYOUT_REVIEW_2026-09.md) reproduced a clipped Rename/Remove menu and enlarged-text truncation through the actual component. It expands controls inside the card, reveals the whole selected room, wraps names and sizes rename fields properly. Actual-component owner/viewer fixtures and actual read-only channel-room selection pass without real mutations. All 631 readiness tests, actual assembly, build/typecheck, zero-error lint and unchanged audits passed, followed by exact Vercel verification at `9f173c5` and the three-case production browser proof. Cloud CI `34348226460` completed: Convex ready 12:10:55 UTC, Trigger `20260909.11` deployed 12:12:55 UTC. All 151 numbered requirements are retained, with no duplicates or gaps.
- **Golden card reflow:** [the card/controls repair](GOLDEN_CATALOG_REFLOW_REVIEW_2026-09.md) is verified on the exact production alias at `56be19f`. The same real-browser oracle fails 227 checks on the old layout and passes all 280 collapsed/opened card pairs on the new one, including control focus/hit targets and unchanged shared lock-query behavior. 631 readiness tests and the full build/audits passed; the report separately records the combined-shell termination and clean independent assembly confirmation. Production screenshots were inspected. Seven actual tool destinations open successfully on desktop and phone; their render/owner workflows are not claimed as tested. Cloud CI `34350598740` completed: canonical Convex ready 12:33:03 UTC and Trigger `20260909.12` deployed 12:35:02 UTC.
- **Golden evidence follow-up:** [the separate assurance-panel repair](GOLDEN_EVIDENCE_REFLOW_REVIEW_2026-09.md) removes huge empty neighboring panels, clips neither blocker reasons nor foundation text, and fixes detached descriptions. Release `aa96282` passed all 631 tests, actual assembly, build/typecheck, zero-error lint and unchanged audits. Exact Vercel alias and both production browser proofs pass; screenshots were inspected. Cloud CI `34352067117` completed: Convex ready 12:50:00 UTC, Trigger `20260909.13` deployed 12:51:58 UTC. Broader module/output qualification remains open.
- **Studio-assets follow-up:** [the inventory-state/layout repair](STUDIO_ASSETS_STATE_AND_LAYOUT_REVIEW_2026-09.md) reproduces false unloaded counts, stale approvals after failed refresh and channel-only clips labelled portable. Three local passes cover all five sections, eight access/data states, real page/provider controls and failure/retry/cancellation boundaries without external mutations. Release `9ca4a62` passed all 631 tests, actual assembly, build/typecheck, zero-error lint and unchanged audits. Exact Vercel alias and five live layouts plus six navigation journeys pass, with inspected screenshots and no private-inventory requests. Cloud CI `34356638875` completed: Convex ready 13:35:00 UTC, Trigger `20260909.14` deployed 13:36:55 UTC. Real owner workflows and automatic library promotion remain open.
- **Shared top-bar follow-up:** [the contrast repair](TOPBAR_CONTRAST_REVIEW_2026-09.md) retains the subtle gradient on an opaque base. Release `c58adc6` passed all 631 tests, actual assembly, build/typecheck, zero-error lint and unchanged audits, and is verified on the exact Vercel alias. The actual-page pixel oracle rejects all eight old cases and reports zero content bleed in all eight production cases after a documented Library-data readiness correction; six navigation journeys pass and screenshots were inspected. Cloud CI `34359472209` completed successfully: Convex ready 14:05:50 UTC, Trigger `20260909.15` deployed 14:07:53 UTC. [Runtime deployment research](RUNTIME_DEPLOY_EFFICIENCY_REVIEW_2026-09.md) found that pinned Trigger native mode bypasses its dry-run branch and its JS content hash omits later build layers; no unsafe diagnostic or speculative deployment-skip change was made.
- **Lo-Fi reference follow-up:** [three distinct visual passes](LOFI_REFERENCE_LAYOUT_REVIEW_2026-09.md) replace the decorative archive map/status rail with the exact uncropped still, four composition cues and working source/catalog controls. Default copy falls from 331–334 to 67 words while release/provenance boundaries remain in one native disclosure. Release `41a4b76` passed all 631 tests, actual assembly, build/typecheck, zero-error lint and unchanged audits. The exact Vercel alias and five production reference cases, six navigation journeys and eight header cases pass; screenshots were inspected. Cloud CI `34362344177` completed successfully: Convex ready 14:26:25 UTC, Trigger `20260909.16` deployed 14:28:25 UTC. This does not qualify paid Lo-Fi generation or alter channel identities/thumbnails; the enlarged-phone bottom-dock follow-up is separately verified at `2093d7e` above.
- **Accounting prerequisite follow-up:** [the OpenRouter usage repair](OPENROUTER_USAGE_NORMALIZATION_REVIEW_2026-09.md) reproduces flat reasoning being charged twice and native nested reasoning losing its warning. Local client tests preserve inclusive cost exactly once and retain known cost on malformed usage; held metadata/stage-handler fixtures additionally block the next purchase. The combined held checkpoint passed all 644 tests, assembly, build/typecheck/lint/audits. A [production-only candidate](OPENROUTER_ACCOUNTING_RELEASE_2026-09.md), `640d584`, excludes the title/lease/structured-output draft. Its frozen client passes the 20-case regression and exact replay of 30 historical production-format responses ($0.0844425, zero live dispatches); the other 36 structured-output experiments stay separately held. Its own full gate passed all 632 tests, real assembly, build/typecheck/lint/audits. Exact Vercel alias and six live navigation journeys pass. Cloud CI `34366556622` completed: Convex ready 15:06:36 UTC, Trigger `20260909.17` deployed 15:08:40 UTC. The broader finite metadata envelope remains open. No provider call or change to model, prompt, token limit or old receipt was made.

- **Retention deletion contract:** [the storage repair](RETENTION_DELETE_CONTRACT_2026-09.md) reproduces and fixes HTTP-success/per-object-failure being reported as completed cleanup. Exact acknowledgements, partial-count recovery and cross-run boundaries are covered by real helper/certificate tests; a live R2 probe created and removed only three isolated text fixtures, leaving no fixtures behind. The idle cleanup entry point requests two vault services instead of 20. Pre-delete lease/release/owner-lock revalidation and a real publication-to-cleanup lifecycle remain open; this does not complete weekly preparation or retention.
  Scoped release `139337e` passed cloud CI `34338714736`: canonical Convex ready at 10:22:24 UTC and Trigger `20260909.7` deployed at 10:24:23 UTC. Its successor `134f9e7` adds the pre-delete-authority repair and passed all 629 direct tests, real hermetic assembly, build/typecheck/lint, all structural audits and 29 Python worker tests in an isolated checkout. That successor is verified on Vercel and completed cloud CI `34342445181`: Convex ready at 11:04:18 UTC, Trigger `20260909.8` deployed at 11:06:17 UTC. The held title/Salad work remains excluded.

- **Title inspection UI:** [the read-only run-stage review](TITLE_REVIEW_UI_2026-09.md) connects the saved title decision to a compact selected/alternate/candidate view using the existing subscription. Actual module/recovery outputs feed its adapter; real-component browser replay uses the previously paid Chalk decision without new spend. Malformed/legacy/title-drift states and 200% mobile text are covered; an observed 8px overflow was fixed at the pipeline grid constraint. The inert-output audit returns to its unchanged baseline. This is not deployment of the title-generation draft, a new quality certificate or completion of the wider page overhaul.
  UI-only releases `84c1478` and `f7e954b` were verified on the exact production alias. The live enlarged-text check exposed additional clipping in the outer run grid despite a passing document-width check; a stricter element-boundary check failed before the one-line parent-grid fix and passed on production afterward without injected styles. Desktop/mobile/200% real run inspection preserves legacy data and one media subscription. Full local combined draft suite: 639 direct tests plus actual hermetic assembly passed. Both releases exclude the unqualified paid title-generation changes; cloud CI/deployment completion is tracked separately in the report.

- **First accepted checkpoint:** commit `ba9d389` was verified on the exact Vercel production alias; [CI 34289436687](https://github.com/daniels-project-space/youtube-studio-ai/actions/runs/34289436687) passed the quality gate and deployed Convex/Trigger. Live browser inspection confirmed no global Library owner trigger, a remaining Settings management trigger, visible refreshed thumbnails, and no page errors. Authenticated-owner behavior was separately tested with a session fixture; that is not a real OAuth lifecycle proof.
- **Vault boundary:** eight concurrent scoped bootstrap callers produce one vault read, failed reads remain retryable, deployment overrides win, and logs do not expose returned secret values. The general bootstrap does not acquire Salad credentials; Salad requests its own vault namespace.
- **Salad foundation:** [live R2/runtime inventory](SALAD_R2_RUNTIME_INVENTORY_2026-09.md) locates all 97 files in the three existing packs and documents the real worker endpoints. This proves inventory, not usable GPU inference. OCI activation, Qwen pack discovery, Music3 memory fit, H3 permitted capacity, durable bulk coordination, and real output qualification remain open.
- **Fresh Salad wiring audit:** [9 September live checks](SALAD_BULK_WIRING_AUDIT_2026-09-09.md) verified the three manifest hashes, all 97 declared object sizes, existing ERNIE registry digest and working vault pull credentials. The production controller, fleet-wide three-GPU leases, prepared-media adoption and confirmed shutdown are still missing. H3's existing country/resource filter currently reports zero capacity; bounded Qwen discovery found no TTS pack in the checked prefixes. Header-only inspection puts Music3's BF16-equivalent tensors at 21.87 GiB, not the whole 57.4 GB storage pack; actual 3090 residency, long-duration performance and the native 44.1 kHz/application 32 kHz delivery mismatch remain unqualified. No GPU was launched and no stored pack was recreated.
- **Title baseline:** [current title-module audit](TITLE_MODULE_BASELINE_2026-09.md) preserves the real call chain, reproduced false-pass and contradictory fallbacks. [Eight real source packets are now frozen](TITLE_CORPUS_SOURCE_AVAILABILITY_2026-09.md), including five full narrations, with file/source/narration hashes. They are source-backed regressions, not exact historical invocation replays: missing context remains explicit. That source-freeze checkpoint did not establish live generation or output improvement; the subsequent pilot is recorded separately below.
- **Title research and live calibration:** the [source-cited research report](TITLE_MODULE_RESEARCH_2026-09.md) separates platform guidance, experimental headline findings and unproven engineering proposals. The [first real baseline and unchanged repeat](TITLE_MODULE_PILOT_2026-09.md) exposed an unsupported selected promise and variation on identical input. [The first oracle calibration](TITLE_ORACLE_RESULTS_2026-09.md) exposed schema, metaphor, identity and evidence-subtype errors. [The frozen second pass](TITLE_ORACLE_SECOND_PASS_RESULTS_2026-09.md) then produced valid responses in 36/36 calls: seen calibration 46/48 exact factual labels and 28/28 identity checks; six separately authored unseen pairs 22/24 factual labels and 12/12 compatible-identity checks. No unsupported claim was admitted in either stratum; unresolved evidence is still sometimes mislabeled contradicted. Agent-authored expectations are not owner/human preference labels. All live title experiments to date cost $0.18955650 across 66 priced calls. Cost and latency increased in the second pass; no savings, audience/virality gain or broad completion is claimed.
- **Title deployment admission is still open:** the draft removes competing title authority and tests durable package-only recovery, but the [actual-engine paid-admission probe](METADATA_PAID_ADMISSION_FOLLOWUP_2026-09.md) reproduced an existing post-spend-only budget check plus recovery/reservation conflicts if metadata is merely relabeled paid. Production promotion must wait for verified ledger-aware admission and local execution authority; do not bypass those controls or call the draft budget-safe. This hold does not pause unrelated goal work.
  The [exact integration design](METADATA_PAID_ENGINE_DESIGN_2026-09.md) traces shared input construction, read-only immutable-ledger inspection, exact receipt credit, local execution-generation checks and frozen-contract migration. Reusing the existing heartbeat would clear remote-wait state; changing paid classification without a module version would silently change a frozen spend contract. These are required implementation/test boundaries, not completed code.
  The [draft validation checkpoint](TITLE_DRAFT_VALIDATION_2026-09.md) records 632/632 direct tests and actual hermetic assembly, plus the earlier failed assertion and its behavioral follow-up. The unready implementation and raw experiments are committed on a separate local checkpoint branch, not deployed or included in production `main`.
  The subsequent [checkpoint inspection slice](METADATA_CHECKPOINT_INSPECTION_2026-09.md) is now wired to actual metadata execution: one shared input builder, nine-key full-ledger validation, 26 corrupted-state refusal cases, exact package-only recovery and no provider calls for complete replay. This advances the first integration slice; engine budget/credit/lease admission still remains open.
  The next [inline execution-ownership slice](METADATA_INLINE_EXECUTION_LEASE_2026-09.md) now checks the current service-bound run generation before each new claim/request. Tests traverse the actual runner, metadata block, HTTP parser/accounting, authenticated query and stage-write handler: stale workers cannot write final stages, saved responses survive, and a new worker purchases only missing work. The earlier code made four test requests despite revoked ownership; the new code makes zero when already revoked. This remains a local draft, not live proof or completion of the separate budget/credit gate. The unchanged-credit continuation rule above remains active.
  Its validation passed 637/637 direct test files, an actual hermetic assembly, production build, typecheck and scoped lint. The separate structural audit is **not green**: the draft's persisted `titleDecision` store output adds one no-downstream-reader finding. Its receipt is used in checkpoint validation, but its terminal evidence/consumer disposition still needs review; the audit baseline was not raised. The broader release hold is unchanged.
  The subsequent [verified receipt-admission slice](METADATA_INLINE_RECEIPT_ADMISSION_2026-09.md) now reconciles exact immutable receipts before further work, persists recovered charges through the fenced sink, and credits only verified same-ledger work against its remaining reservation. Actual metadata/HTTP/parser/handler tests use a clearly labelled test-only paid envelope, cover stale-parent and already-counted recovery, and make no external calls. Production request bounds, per-request allowance and frozen-contract migration remain open; no paid flag or rollout was enabled. All earlier UI/module/Salad/retention work remains on this list.
  Its final local gate passed **638/638 direct test files**, a real 31.021995-second hermetic assembly, build, TypeScript and scoped lint. Graphify was refreshed. The prior output-audit finding still prevents a fully green structural gate; cost-only projection for held ledgers is also explicitly open. Live health still reports `2c8e64a`, not this draft.
  The subsequent [held-cost recovery slice](METADATA_HELD_COST_RECOVERY_2026-09.md) closes that known-cost projection gap: held/partially unreadable ledgers can record independently valid charges while remaining failed, with no reservation credit, body call or paid retry. Tests traverse the actual metadata and fenced stage handler, including stale/failed writes and later R2 recovery. Unknown/unpriced charges remain unresolved, and production request bounds/migration/output audit still hold the title rollout. No render credits were needed.
  Its final gate passed **638/638 direct test files**, the real 31.021995-second hermetic assembly, TypeScript, scoped lint and production build. The code graph was updated; structural audit counts are unchanged from the prior draft, including its still-open output finding. The live alias remains at `2c8e64a`; this is not a production release claim.
- **Backend checkpoint deployed:** `2792c0e` passed all 622 direct tests, a real 31.02-second assembly render, production build, typecheck, lint (zero errors; 34 existing warnings), structural audits and the 24-defect proof generator. [CI 34291526277](https://github.com/daniels-project-space/youtube-studio-ai/actions/runs/34291526277) passed and deployed canonical Convex at 23:48:39 UTC and Trigger version `20260908.41` at 23:50:38 UTC on 8 September ([deployment h8x3mxd9](https://cloud.trigger.dev/projects/v3/proj_vorkjqmnnpkzoiqqgbuu/deployments/h8x3mxd9)). The exact Vercel production alias then reported `2792c0e`; live Convex introspection confirmed the new child-cost functions. Tests invoke real cost/retention handlers and checkpoint adapters, including duplicate/lost responses, cached versus fresh charges, stale-parent writes, delayed/private release and owner/lease fences. Live read-only inspection found zero enrolled retention records; a real publication-to-cleanup lifecycle remains unproven. No paid GPU qualification or cleanup was performed as a test.
- **Healer follow-up remains distinct:** [the run-cost projection fix](RUN_COST_ROLLUP_FOLLOWUP_2026-09.md), committed as `d7642ef` and deployed in the later `9caa54c` checkpoint below, floors covered run summaries from durable charges and preserves exact scheduled-completion replay. The real-engine regression now persists $0.50 into both run and analytics despite a stale $0.00 worker result, without a second paid attempt. Eleven targeted suites and the subsequent 625-test combined local gate passed. Worker-return values, active-run incremental projections and protective factual-review blocking summaries remain explicitly outside this slice; it must not be folded into the earlier release claim.
- **Independent release verification:** [real-render parity 34291526241](https://github.com/daniels-project-space/youtube-studio-ai/actions/runs/34291526241) passed all five assembly scenarios with identical outputs. Live cost reconciliation correctly returned incomplete/no-attempt evidence for an absent dispatch and rejected a viewer identity; release-check discovery returned no due records. A browser check showed Chalk & Compound's $1.18 run total matching its stage ledger and no page errors, but exposed an old thumbnail on run detail despite the refreshed Library image. That defect led to the separately verified repair below; it was not silently counted as a passed visual check.
- **9 September run-detail repair, production verified:** commit `9caa54cab53bb866f2afd1ed7268c4fcb27df885` was verified through the exact production alias health response and real browser checks, with no auth fixture or request passthrough. Desktop (1440×1000) and mobile (390×844) show the same `TAX DECODED` candidate as Library (`js7644m42q9vecb387hqder7md8e0fm4`). Source and historical-original links work; the source action sits below the artwork rather than over its branding. The current image requests its URL once, and the original image is neither mounted nor requested until history opens. There were no page errors or horizontal overflow; Library still has no global owner trigger. Screenshots were captured and inspected at `/tmp/ysa-ui-audit/run-chalk-9caa54c-production-desktop.png` and `/tmp/ysa-ui-audit/run-chalk-9caa54c-production-mobile.png`. Actual viewer-handler and real-component tests cover provenance, hydration, rejected candidates, LoFi source fidelity, ownership and footer placement. This proves the thumbnail projection/control repair, not full legacy-video quality or the whole-page redesign.
- **9 September combined release completed:** [CI 34294553326](https://github.com/daniels-project-space/youtube-studio-ai/actions/runs/34294553326) passed all **626 direct tests**, the actual 31.02-second 1920×1080 assembly render using hermetic synthetic media (not a live provider/R2 qualification), typecheck, lint, worker/dependency checks, structural audits and defect proofs. Final local production build passed, with zero lint errors and 33 existing warnings. The deployment log confirms canonical `astute-camel-689` ready at **00:32:45 UTC**, followed by Trigger **`20260909.1`** completed at **00:34:38 UTC** ([deployment muqfzy7s](https://cloud.trigger.dev/projects/v3/proj_vorkjqmnnpkzoiqqgbuu/deployments/muqfzy7s)). A post-Convex-deployment read-only check confirmed the retained Chalk run's $1.18 total matches all 16 valid, unique stage rows; the injected outage remains tested through real handlers/engine in the controlled fixture, not by breaking production. Graphify was updated after the final source change. No paid GPU/model job, publishing operation, retention deletion or test mutation was used for this release.

## Current assessment

- The live execution core is real and well tested, but the catalog, module contracts, and production callers are not yet uniformly the same system. The August audit found working, unwired, and catalog-only modules mixed together.
- `metacraft` is already substantially better than the legacy metadata path: seven distinct title frames, live YouTube suggestions and competitor titles, factual lint, a feed judge, one repair attempt, and a retained runner-up. It is not finished.
- The title path still has a contradictory legacy fallback that asks for 60–90 characters, repeats unsupported CTR folklore, and can silently provide a lower-quality package after the stricter path fails. That destroys consistency exactly when the primary route is under stress.
- Title quality is judged mostly inside the same model family that generated it. Deterministic truth/shape checks exist, but the click-quality oracle still needs calibration against owner choices and real post-package watch-time observations.
- A single 25–76 character lint band is too blunt for every format. Search-led explainers, terse motivational titles, serialized lore, children’s learning, Lo-Fi, and Shorts need different tested bounds without allowing each channel to invent arbitrary rules.
- The system records alternates and has a title-swap path, but experiments are not yet a first-class title/thumbnail package with a statistically useful outcome ledger on every eligible channel.
- Existing fleet audits already catch inert inputs, single-constant convergence, silent degradation, and reasoning-token starvation. They are a strong base, but each remaining module still needs a domain oracle and real before/after corpus.
- Trigger and Convex usage has several proven efficiencies already (request memoization, immutable receipts, bounded retries), but no complete per-module call/cost/latency ledger yet proves which steps can be eliminated or consolidated.
- The UI has had multiple large visual passes, but hierarchy is still inconsistent: some pages expose global owner controls unrelated to the page, some panels repeat evidence prose, and some progress views spend too much area on decoration instead of state and next action.

## Research-derived operating principles

- YouTube’s own guidance says titles should be accurate, succinct, and front-load the important words; searchable and intriguing titles are both valid depending on audience and discovery surface. It recommends evaluating Home/Suggested and Subscription-feed CTR in the first 24 hours on videos with above-average impressions.
- YouTube’s current native experiment chooses among up to three title/thumbnail combinations by **watch-time share**, not CTR alone. Our oracle therefore must reward a click that the opening actually fulfills, not maximum clickbait in isolation.
- YouTube permits 100 characters, but that is a storage ceiling, not a quality target. The module must learn tested format-specific working ranges and mobile-front-load scores.
- YouTube Data API reads should request only required `part` and `fields`, reuse ETags where helpful, and avoid redundant search calls because every request consumes quota.
- Trigger child work should use explicit queues, attempt-scoped idempotency when a retry genuinely needs a new child run, and batch waits rather than parallel `triggerAndWait` calls.
- Convex queries should be deterministic and cacheable, large reads should use selective indexes or pagination, and related database work in actions should be consolidated instead of paying for sequential `runQuery`/`runMutation` calls.
- Apple’s current design guidance emphasizes hierarchy, consistency, adaptive layout, progressive disclosure, legible type, and strong text/background contrast. “Apple-like” here means those operating qualities—not decorative imitation.

Primary sources:

- https://support.google.com/youtube/answer/12340300
- https://support.google.com/youtube/answer/16391400
- https://support.google.com/youtube/answer/57404
- https://developers.google.com/youtube/v3/getting-started
- https://trigger.dev/docs/triggering
- https://trigger.dev/docs/queue-concurrency
- https://trigger.dev/docs/idempotency
- https://docs.convex.dev/understanding/best-practices
- https://docs.convex.dev/functions/actions
- https://docs.convex.dev/database/reading-data/indexes/
- https://developer.apple.com/design/human-interface-guidelines
- https://developer.apple.com/design/human-interface-guidelines/layout
- https://developer.apple.com/design/human-interface-guidelines/typography

## Individually tracked work items

### A. Measurement harness and invariants

- [ ] **01 — Freeze the comparison corpus.** Select real retained inputs from every active format plus deliberately unfamiliar niches; hash inputs and expected channel identity so later passes cannot cherry-pick fixtures.
- [ ] **02 — Establish a module scorecard schema.** Record output validity, oracle score, false-pass/false-reject counts, wall time, provider calls, input/output tokens, Trigger runs/waits, Convex calls/rows read/written, and estimated cost.
- [ ] **03 — Capture the current baseline.** Run every non-thumbnail module on the frozen corpus before changing it and retain raw outputs, logs, receipts, and exact code/model/config identities.
- [ ] **04 — Prove every oracle can fail.** Inject at least one realistic corruption per promised capability and require the responsible module gate to reject it with a useful root-cause code.
- [ ] **05 — Calibrate false rejection.** Run owner-approved/golden outputs through each oracle and fix the oracle before changing production when known-good work is rejected.
- [ ] **06 — Add isolated module runners.** Each module must execute from only its declared inputs with provider calls stubbed or live according to the test tier; undeclared store reads fail the test.
- [ ] **07 — Add pairwise contract tests.** Test every producer→consumer artifact boundary and prove that reorder/removal produces a compile-time or fail-closed runtime error rather than generic degradation.
- [ ] **08 — Add whole-pipeline mutation tests.** Remove, duplicate, reorder, and corrupt modules in representative pipelines and require the compiler/runner to diagnose the exact broken contract.
- [ ] **09 — Add a no-overwrite ledger.** Record artifact ownership and reject a module that silently replaces another module’s output without an explicit revision contract.
- [ ] **10 — Make the scorecard a CI artifact.** Store baseline/current deltas and fail only on calibrated regressions, never on invented absolute vanity scores.

### B. Title and metadata module — first major implementation pass

- [ ] **11 — Separate title quality from metadata packaging.** Give title generation, title judging, descriptions/tags, and pinned comments separate typed contracts so a description failure cannot downgrade a winning title.
  **Partial (10–11 September):** a package failure after a successful title decision now returns the selected title with explicit `packageFallback` evidence and a deterministic factual package; it no longer throws into legacy title selection. The metadata block now emits the indexed `titleDecision` receipt consumed by the run-stage review UI, with a canonical SHA-256 content fingerprint checked again by the browser adapter. Independent package contracts and release-bound decision admission remain open.
- [ ] **12 — Replace one universal length band with tested format profiles.** Define bounded profiles for searchable long-form, browse-led long-form, serialized/lore, motivational speech, children/quiz, music/Lo-Fi, and Shorts; keep YouTube’s 100-character hard ceiling everywhere.
- [ ] **13 — Score front-loaded information.** Measure whether the person/entity, tension, consequence, or search phrase survives the first mobile-visible segment instead of treating character count as a proxy.
- [ ] **14 — Generate meaningfully different hypotheses.** Require candidate semantic-distance across search intent, direct verdict, contradiction, stakes, transformation, specificity, and curiosity—reject paraphrase-only tournaments.
- [ ] **15 — Make frame selection channel-aware without hard-coding niches.** Match frames to audience intent, discovery surface, tone, video format, and script evidence through typed capabilities.
- [ ] **16 — Add a title novelty detector.** Reject fleet-wide syntactic repetition, recurring filler, and channel-to-channel phrase leakage while permitting a channel’s proven signature format.
- [ ] **17 — Strengthen factual grounding.** Validate names, quantities, causal claims, quotations, superlatives, and implied outcomes against the fact-checked script/evidence packet, including spelled numbers and aliases.
- [ ] **18 — Add promise-to-opening entailment.** Independently score whether the first 15–30 seconds actually begin fulfilling the title; a high-click title that breaks this contract cannot win.
- [ ] **19 — Build a calibrated pairwise ranker.** Compare candidates head-to-head against the real feed and owner selections, then measure agreement, tie rate, positional bias, and stability over repeated runs.
  **Partial (10–11 September):** the primary metacraft judge now has a strict deterministic admission boundary for complete finite rankings, explicit directness, integer unique indexes, and bounded scores; malformed or transport-failed responses retry once and fail loudly before ancillary packaging. See [the title judge admission review](TITLE_JUDGE_ADMISSION_REVIEW_2026-09.md). Calibration against a held-out corpus and owner/watch-time outcomes remains open.
- [ ] **20 — Preserve searchable and intriguing routes.** Never force every channel into curiosity-gap clickbait; choose the route using intended discovery surface and audience evidence.
- [ ] **21 — Remove contradictory legacy doctrine.** Delete unsupported “long titles lift CTR” folklore, 60–90-character drift, automatic caps/niche suffixes, and any fallback rule that conflicts with the primary module.
- [ ] **22 — Make fallback quality-equivalent.** A provider fallback must use the same candidates, deterministic lint, promise contract, and calibrated judge—or fail loudly and preserve the last valid title.
  **Partial (10–11 September):** the no-provider path no longer restores an unevaluated planned title; it logs the hold-out and preserves the deterministic topic title. A configured-provider error now fails closed in `metadataOptimized` rather than falling into the unjudged legacy tournament/critique loop. The shared candidate/judge contract and browser receipt are now wired; calibrated fallback equivalence and durable release admission remain open.
- [ ] **23 — Keep planned titles as candidates only.** Re-verify every scheduled and topic-bet path so no pre-script title regains precedence after judging.
- [ ] **24 — Produce up to three experiment-ready packages.** Keep truly diverse, independently passing title/thumbnail pairings with immutable package IDs rather than a winner plus an unlabeled spare string.
- [ ] **25 — Measure by watch-time share.** Integrate eligible YouTube title/thumbnail experiments or an equivalent confidence-qualified observation ledger; use CTR diagnostically but select learning outcomes by fulfilled watch time.
- [ ] **26 — Guard experiment eligibility.** Respect YouTube advanced-feature, content, privacy, and format restrictions and never treat ineligible videos as failed tests.
- [ ] **27 — Use audience evidence economically.** Cache normalized suggestions/competitor evidence by query+locale+freshness window and reuse it across title, topic, SEO, and thumbnail-promise planning.
- [ ] **28 — Reduce title-path model calls.** Baseline the current generator, judge, package, and comment calls; consolidate only prompts that share fate/context, parallelize independent work, and require equal-or-better corpus scores before accepting savings.
- [ ] **29 — Calibrate temperature and token ceilings.** Measure title diversity, judge stability, JSON validity, latency, and cost for each route; remove headroom or reasoning only where real hard cases still pass.
- [ ] **30 — Add title-specific adversarial tests.** Include sensational-but-false, correct-but-boring, vague, overloaded, repetitive, truncated, mismatched-tone, spoiler-heavy, and policy-risky candidates.
- [ ] **31 — Validate multilingual titles.** Test length, casing, word segmentation, entity grounding, and front-loading in every supported language instead of applying English regexes blindly.
- [ ] **32 — Publish a before/after title report.** Show corpus outputs, owner-choice agreement, promise-match, diversity, length/front-load distribution, calls, cost, and latency with the exact revision and model route.

### C. Every remaining module, inspected and improved separately

- [ ] **33 — Topic Intel / Topicraft.** Measure novelty, demand evidence, saturation, channel fit, provenance, and title leakage; eliminate duplicate YouTube research already gathered elsewhere.
- [ ] **34 — Episode Graph.** Test causal order, retention beats, duration feasibility, and scene coverage across documentary, essay, story, quiz, music, and short formats.
- [ ] **35 — Self-Contained Story.** Prove the handoff includes every fact, identity rule, and render requirement needed downstream without hidden store reads.
- [ ] **36 — Casefile Documentary.** Validate source-to-claim traceability, uncertainty language, chronology, and no unsupported dramatization on real and adversarial cases.
- [ ] **37 — Evidence Shot Map.** Require every visualized claim to map to admissible evidence and detect decorative or contradictory shots.
- [ ] **38 — Source-Bound Story Spine.** Measure factual coverage, pacing, and timed handoff completeness; consolidate duplicate source parsing with the evidence packet.
- [ ] **39 — Editorial Evidence Packet.** Deduplicate network/database retrieval, normalize citations once, and make every consuming module read the same immutable factual core.
- [ ] **40 — Cinematic Case Sequence.** Validate shot continuity, geographic/period accuracy, evidence density, visual variety, and achievable render instructions.
- [ ] **41 — Learning Contract.** Test objective alignment, retrieval practice, difficulty progression, age/language fit, and measurable learning outcomes.
- [ ] **42 — Children’s Show Bible.** Validate curriculum, character continuity, participation cadence, safeguarding, developmental fit, and non-generic identity.
- [ ] **43 — Show Bible + Crew.** Wire every director/cinematographer/editor/composer/critic output through declared contracts and remove inert doctrine/knobs.
- [ ] **44 — Script + Hook.** Measure spoken naturalness, opening retention promise, narrative momentum, factual coverage, repetition, duration accuracy, and channel voice across formats.
- [ ] **45 — Guard Gates.** Calibrate advertiser safety, factual safety, child safety, originality, and policy classifiers in both directions; remove duplicate scans that inspect the same artifact with no incremental signal.
- [ ] **46 — Narration / Voicecraft.** Test cast fit, intelligibility, pronunciation, pacing, silence, loudness, language, retry boundaries, and reusable audio caching without replacing required human audition evidence.
- [ ] **47 — Music / Scorecraft.** Test brief fit, licensing provenance, loudness, loopability, duration strategy, voice masking, originality, and reuse policy; avoid generating music before timing is stable.
- [ ] **48 — Scene Compiler.** Validate deterministic layout, identity preservation, text safety, aspect adaptation, and render parity across unfamiliar art directions.
- [ ] **49 — Novita Render Farm.** Measure queue wait, GPU utilization, warm reuse, checkpoint recovery, spot interruption, idle shutdown, artifact integrity, and cost per accepted second.
- [ ] **50 — Imagecraft Novita.** Either wire the catalog entry into a real executable capability with independent QA or mark/remove it; never leave a parallel dead engine.
- [ ] **51 — Videocraft Novita.** Prove LTX input/output contracts, motion quality, first/last-frame continuity, retries, checkpointing, and exact provider/cost receipts.
- [ ] **52 — Lo-Fi Loop.** Test exact source-frame identity, seamless loop metrics, 1–8-hour scaling, audio/video drift, 4K delivery, and the rule that the visual scene is not generically regenerated.
- [ ] **53 — Lore Short.** Test canon/evidence grounding, watercolor/illustration identity, vertical composition, narrative completeness, and short-duration pacing.
- [ ] **54 — Quiz / Quiz Year / Quiz Short.** Resolve buildability/licensing truth, validate answers and distractors, difficulty, pacing, accessibility, deterministic metadata, and private-release boundaries.
- [ ] **55 — General Visuals.** Make channel identity, story coverage, shot variety, entity continuity, and source/reuse policy typed requirements instead of prompt suggestions.
- [ ] **56 — Studio Asset Library.** Enforce channel-scoped access, provenance, retention, originality cadence, reuse ceilings, and cost savings with no cross-channel identity leakage.
- [ ] **57 — Cinecraft.** Validate shot language, realism, continuity, performance, editability, and useful narrative coverage rather than isolated pretty frames.
- [ ] **58 — Documotion.** Test evidence-first motion, maps/documents/timelines, legibility, restrained effects, and period/location correctness.
- [ ] **59 — Motioncraft.** Wire or remove the current catalog-only path; test that motion graphics clarify information, fit the cut, and do not overwrite captions or identity.
- [ ] **60 — Speechcraft.** Validate speaker likeness policy, speech-to-visual synchronization, motivational pacing, quotation provenance, and non-generic speaker/channel presentation.
- [ ] **61 — Data-Viz Inserts.** Preserve the existing 19/19 legitimate and 181/181 adversarial calibration, then add visual legibility, unit accuracy, narration timing, and design-system fit.
- [ ] **62 — Captions + Presentation.** Test word timing, safe areas, reading speed, hierarchy, collisions, languages, Shorts/16:9 variants, and restraint; remove redundant render passes.
- [ ] **63 — Assembly / EDL.** Replace the documented-but-orphaned contract with the executed renderer contract; prove idempotency, checkpoint recovery, LUFS, A/V sync, transitions, no dead air, and render parity.
- [ ] **64 — SEO Metadata beyond titles.** Measure descriptions, chapters, tags, pinned comments, policy compliance, language, duplication, and actual search value; stop producing fields whose measured value is negligible.
- [ ] **65 — Artifact + Shot Gates.** Consolidate overlapping image/video judges, route defects to the responsible module, and prove final acceptance cannot pass with missing scenes or stale artifacts.
- [ ] **66 — Final Master Story Coverage.** Watch the complete output, score every narrated claim/beat against on-screen coverage, and require re-review after any repaired artifact changes.
- [ ] **67 — Whiteboard / Drawn Cinema.** Test that the full story is drawn, not only early panels; enforce stroke/build continuity, hand timing, identity, legibility, and no unexplained static gaps.
- [ ] **68 — Motion Comic.** Test page/panel continuity, character identity, speech/narration alignment, composition, motion restraint, and complete story coverage.
- [ ] **69 — Ship.** Validate exact channel identity, private-first policy, metadata/media checksums, resumable upload, provider verification, idempotency, and no duplicate publish or notification-induced false failure.
- [ ] **70 — Channel Planner.** Test schedule feasibility, format-specific duration/workload, diversity, dependencies, batch preparation, cost caps, and no invented analytics.
- [ ] **71 — Shorts.** Validate vertical-safe storytelling, ≤format duration, hook speed, subtitle density, source originality, metadata rules, and no long-form assumptions.

### D. Cost, latency, Trigger, Convex, and connector consolidation

- [ ] **72 — Build a provider-call graph.** Attribute every external call to module, artifact, cache key, attempt, and accepted/rejected output; unknown cost is a failing measurement.
- [ ] **73 — Eliminate duplicate research calls.** Share immutable, freshness-bound YouTube/audience/evidence packets between topic, title, SEO, script, and packaging consumers.
  **Partial (10 September):** `metacraft.youtubeSuggest` and `fetchCompetitorTitles` now normalize equivalent seeds, coalesce concurrent requests, reuse successful public evidence for a bounded five-minute TTL, return defensive copies, and leave failures retryable. Focused offline tests prove one request for concurrent/repeated autocomplete and competitor searches, plus a fresh request after a simulated 503. The broader cross-module audience/evidence packet and quota measurement remain open.
  **Additional partial (10 September):** the shared `youtubeData` public `search.list` and competitor `videos.list` readers now normalize search keys, coalesce concurrent equivalent calls, reuse successful results for the same bounded TTL, return defensive copies, and keep failures retryable. A focused fetch-seam test proves one low-level request per concurrent search/detail batch. Cross-module packet persistence and quota accounting remain open.
- [ ] **74 — Audit YouTube quota shape.** Minimize `part` and `fields`, batch video IDs, cache stable responses/ETags, and measure quota units per completed video before and after.
  **Partial (10 September):** [the request-shape review](YOUTUBE_DATA_QUOTA_SHAPE_REVIEW_2026-09.md) now uses the required/minimal parts plus `fields` for search, competitor detail, live video stats, and channel rollups. Duplicate/blank IDs are removed before 50-ID batching, and a defensive process-local observation records attempted list requests, estimated one-unit list costs, cache hits, and in-flight coalescing. Fetch-seam tests prove the exact shapes and normalized batches. `fields` improves transfer/parsing, not the method quota charge; durable cross-module quota accounting, ETags, and per-completed-video measurement remain open.
- [ ] **75 — Audit Trigger task granularity.** Merge tasks only when they share retry/failure fate; preserve separate tasks where checkpointing, queueing, cost attribution, or independent retries matter.
- [ ] **76 — Audit Trigger waiting/concurrency.** Replace accidental active polling and unsafe parallel waits with checkpoints, batch waits, explicit resource queues, and bounded backoff.
- [ ] **77 — Audit idempotency scopes.** Use run/global/attempt keys intentionally and prove retries neither reuse a permanently failed child nor double-spend successful work.
- [ ] **78 — Audit Convex round trips.** Count `runQuery`/`runMutation`/`runAction` per pipeline stage; combine consistent related reads/writes and call plain helpers inside the same runtime.
- [ ] **79 — Audit Convex query bounds.** Replace broad collects/filters with selective indexes, `take`, or pagination; measure rows/bytes read and index-maintenance trade-offs.
- [ ] **80 — Audit reactive UI queries.** Remove duplicate subscriptions and frequently changing arguments that defeat cache reuse; keep one source of truth per visible datum.

  **Measured and released case (9 September):** [the combined run-media query](RUN_MEDIA_QUERY_EFFICIENCY_2026-09.md) reuses the exact existing provenance/LoFi/master selection rules and removes duplicate asset subscriptions and script/SEO probes. On five actual production runs, old combined response text fell from 43,374 to 8,639 serialized characters with exact current-thumbnail and raw-asset parity. Desktop/mobile production checks confirm one new subscription, zero old subscriptions, visible current artwork and working source/history controls at Vercel revision `94ee38e`; canonical backend `521dcce` passed all 626 direct tests and deployed. Logical indexed reads and serialized characters are not invoices or wire bytes. Live sealed/LoFi branches remain covered by handler fixtures rather than these five legacy run samples; do not extrapolate a fleet-wide billing saving.

- [ ] **81 — Audit model response reuse.** Verify cache keys include model, prompt, schema, version, and relevant channel identity while excluding incidental run IDs that prevent safe reuse.
- [ ] **82 — Add spend-before-value gates.** Resolve deterministic requirements, existing artifacts, locks, and eligibility before paid model/render calls; prove gates do not suppress valid work.
- [ ] **83 — Publish an efficiency delta report.** For every accepted consolidation, report provider calls, Trigger runs/wait time, Convex calls/rows, latency, cost, cache hit rate, and quality delta.

### E. Novel-channel proof without manual substitution

- [ ] **84 — Define a genuinely unfamiliar channel brief.** Select a niche, visual grammar, duration pattern, audience, and format not represented in the frozen corpus; do not hand-author its pipeline.
- [ ] **85 — Run automated channel creation unchanged.** Capture the architect’s research, module selection, configuration, art, connector checks, and bounded test-render decision without helping it choose.
- [ ] **86 — Tail the entire test render.** Observe real Trigger/Convex/provider state and receipts; intervene only to repair a discovered root defect or unblock a truly unavailable external service.
- [ ] **87 — Preserve the failed attempt.** Keep exact artifacts/logs/costs from every failure before fixing code so the rerun is an honest before/after, not a rewritten history.
- [ ] **88 — Rerun from the proper boundary.** Resume/checkpoint when the contract permits; restart only when the repaired upstream artifact invalidates downstream work.
- [ ] **89 — Validate each module alone after repair.** Reproduce the failure in its isolated runner and add a falsifiable regression before rerunning the integrated channel.
- [ ] **90 — Validate the complete new channel visually and functionally.** Inspect avatar, banner, tile, channel theme, title, script, all visuals, narration, edit, captions, metadata, progress UI, final master, and private-release evidence.
- [ ] **91 — Produce the final novel-channel scorecard.** Compare against baseline and target on quality, consistency, calls, cost, speed, recovery, and truthful UI state.

### F. Whole-Studio modern minimal design pass

- [ ] **92 — Inventory every route and subpanel.** Record purpose, primary action, required evidence, current density, duplicate prose, dead controls, and mobile behavior before restyling.
- [ ] **93 — Establish one restrained visual system.** Define modern type scale, spacing grid, radii, border/material layers, contrast, semantic color, gradients, icon rules, motion tokens, and reduced-motion behavior.
- [ ] **94 — Increase useful text legibility.** Raise undersized body/metadata text, shorten copy, reduce headline theater, and preserve hierarchy at narrow and large-text sizes.
- [ ] **95 — Remove unrelated global controls.** Do not show owner, render, publish, or channel actions on pages where they do not apply; surface permissions only at the consequential action.
- [ ] **96 — Redesign Studio overview.** Use real expandable widgets for issues, runs, channels, schedule, analytics, spend, and master controls; no fake metrics or overlay prose.
- [ ] **97 — Redesign Channels and categories.** Keep tiles compact, category-aware, identity-rich, and free of multi-channel duplicates; give each channel a distinct art system derived from its name and format.
- [ ] **98 — Redesign every channel detail tab.** Make current plan, assets, modules, history, YouTube connection, schedule, scripts, visuals, SEO, narration, retention, and locks directly understandable and actionable.
- [ ] **99 — Redesign New Channel.** Compress copy, make decisions visual and sequential, show real staged progress and QC, and keep automated creation truthful about external waits/failures.
- [ ] **100 — Redesign Production and run detail.** Use a compact stage rail, live artifact previews, elapsed/cost/error state, responsible-module routing, and expandable evidence instead of tall decorative progress panels.
  **Verified narrow correction (9 September):** [production evidence](RUN_MEDIA_QUERY_EFFICIENCY_2026-09.md) records commit `2c8e64a`, full CI and Convex/Trigger deployment, exact Vercel alias and inspected desktop/mobile screenshots. Successful and skipped stages are now reported separately instead of falsely called “Verified.” Current-thumbnail/source/history behavior still passes. This does not close the whole-page redesign or certify legacy video quality.
- [ ] **101 — Redesign Schedule.** Make every video clickable into its exact channel/run details and distinguish planned, prepared, rendering, QC, ready, scheduled, published, and blocked at a glance.
- [ ] **102 — Redesign Library.** Keep the dense master grid, clear current-thumbnail provenance, compact filters, useful archive controls, and progressive disclosure; never let refresh evidence dominate the collection.
  **Saved-master playback released (9 September):** [Library player review](LIBRARY_PLAYER_REVIEW_2026-09.md) records actual production private-YouTube failures despite retained R2 masters. Revision `d3ddace` passed all 640 direct tests, full build/typecheck/audits/assembly and 24 actual-component/native-browser cases covering precedence, expired URL recovery, per-player retry, native arrow-key seeking and modal focus. Errors use natural height and gallery controls sit below footage. Vercel deployment `6357917078` and the exact alias are verified; direct Library desktop/phone and channel Library playback pass with inspected screenshots. Cloud CI `34396644330` is still in progress; full backend confirmation is pending.
  **Information-quality follow-up:** live playback review still shows legacy `~28M est. views · tag_overlap` and unrelated historical SEO tags. These are not verified YouTube analytics or improved metadata. Trace the heuristic's ownership and presentation, and replace/refine legacy metadata through its real module; do not confuse this playback repair with content/analytics qualification.
  **Run-detail consumer follow-up in qualification:** [Run Media recovery](RUN_MEDIA_RECOVERY_REVIEW_2026-09.md) reproduces real expired Range403 unmounting the video, absent Retry, stale failure state after the same asset ID changes keys, and an expired source link. The repair preserves the native node/time/intent, keys per-asset state, exposes initial-signing retry and follows the actual renewed source. Local native-browser tests pass; isolated-release and production verification remain pending. Inactive expired links and non-video native-error recovery remain distinct unfinished lifecycle work.
- [ ] **103 — Redesign Tools and Golden modules.** Present modules as concise capability cards with real media, bullet outcomes, proof, cost/speed, compatibility, and professional locks; hide implementation prose until expanded.
- [ ] **104 — Redesign Settings, analytics, assets, evidence, and render consoles.** Give each a task-specific layout instead of reusing generic giant cards and empty hero regions.
- [ ] **105 — Replace generic symbols.** Create scalable channel/module-specific icons whose restrained motion communicates function; maintain keyboard, contrast, reduced-motion, and small-size clarity.
- [ ] **106 — Validate three visual passes per screen.** Pass 1 hierarchy/density, pass 2 interaction/responsiveness/accessibility, pass 3 polish/identity/motion; capture desktop and mobile screenshots each pass.
- [ ] **107 — Run an actionable-control audit.** Every visible button/link must work against real data, expose disabled reasons, and reach the expected destination or mutation; remove controls with no real caller.
- [ ] **108 — Run visual regression and performance gates.** Compare screenshots, layout overflow, keyboard focus, reduced motion, console/network errors, Core Web Vitals, and bundle impact before production release.

### G. Weekly Salad fleet and cost-aware recovery

Added from the owner's follow-up on 2026-09-08. These items extend all earlier work; they do not replace title, module, UI, or novel-channel validation. Existing R2 worker bundles and model artifacts are the starting point, and credentials must come from the shared vault.

- [ ] **109 — Inventory existing R2 runtimes.** Locate the ERNIE, Qwen TTS, MiniMax music, and H3 bundles, model manifests, workflows, pinned container images, and prior output receipts; verify identity and digests before reuse.
- [ ] **110 — Resolve the H3 runtime from evidence.** Bind the requested H3 video workload to its exact retained model/workflow; an unrelated video model cannot stand in for it.
- [ ] **111 — Connect Salad through the vault.** Verify the current API key, organization, project, registry credentials, and app-scoped runtime access without exposing credentials in manifests, logs, or browser data.
- [ ] **112 — Aggregate the week's media work across channels.** Freeze approved scripts, narration inputs, shot lists, image references, video prompts, and music briefs into one owner/week order with exact channel/episode/module destinations.
- [ ] **113 — Schedule by real dependencies.** Allow independent narration/music/image work concurrently, release video work only when its input images exist and pass checks, and unblock assembly only when required assets are complete.
- [ ] **114 — Group work by worker image and model.** Reuse one pinned deployment image per tool across replicas and process multiple eligible episodes while that model is warm.
- [ ] **115 — Select exact RTX 3090 and desktop RTX 5090 classes.** Use live Salad class IDs/prices: 3090 for ERNIE/TTS/music, 5090 for H3; validate memory/runtime requirements without silently changing GPU quality or model.
- [ ] **116 — Enforce medium priority.** Persist medium priority in each immutable provider request and reject runtime/config drift.
- [ ] **117 — Enforce three GPUs fleet-wide.** Share the three-node limit across all active model groups and concurrent weekly orders; do not accidentally allocate three per tool. Partial: the existing infrastructure proof runner now reserves from one organization-scoped R2 CAS record before provider creation, with one-shot dispatch and no timed release of uncertain GPUs. Four concurrent live R2 test requests admitted exactly three; stale writes were rejected and all unused test reservations closed. Full repository tests pass, including deterministic dispatch/close races and cleanup recovery. The production weekly dispatcher is not yet wired to this authority; no GPU runtime qualification is implied. See `/home/ubuntu/salad-media-infra/docs/FLEET_CAPACITY_2026-09.md`.
- [ ] **118 — Scale to useful remaining work.** Allocate one to three replicas according to ready jobs, deadlines, warm-up cost, throughput, and budget; measure whether extra replicas actually shorten completion.
- [ ] **119 — Add durable per-job claims.** Fence completion so worker interruption or queue redelivery cannot cause two accepted outputs or duplicate paid work; expiry must never authorize new paid work without reconciling the prior attempt. Partial (9 September): the actual ERNIE worker and infrastructure proof caller now use immutable request/model bindings, create-only R2 claims and independently saved results. Four-worker contention permits one take. Ambiguous claims remain held; automatic recovery and the production weekly scheduler remain open. See `/home/ubuntu/salad-media-infra/docs/DURABLE_ERNIE_JOBS_2026-09.md`.
- [ ] **120 — Restore completed work before retry.** Verify R2 checksums and immutable receipts, reuse accepted assets, and retry only missing or recoverably failed jobs. Partial (9 September): the actual ERNIE controller now checks R2 and makes bounded storage-only recovery requests with renewed grants. The worker cannot infer through this endpoint; it reuses a verified completed take, and holds the GPU lock through HTTP cancellation. Full controller/HTTP/failure tests pass, and a rebuilt candidate image is published and CPU-checked. Live GPU deployment remains held on a read-only registry token; genuine paid-output qualification, unattended cross-worker recovery and other media adapters remain open. No synthetic fixture counts as quality proof.
- [ ] **121 — Route results directly into channel/episode R2 paths.** Validate output namespace, content type, size, input/config hashes, and job identity before making the asset available to assembly.
- [ ] **122 — Integrate prepared assets with the existing runner.** Prove scheduled episodes consume their prepared script/TTS/images/video/music and do not regenerate or overwrite those assets.
- [ ] **123 — Drain and stop GPUs as soon as useful work ends.** Stop idle groups immediately after durable results are secured; confirm stopped state, and recover teardown after process failure. Partial: the existing Salad infrastructure proof runner now requires exact zero-instance evidence, saves it before DELETE, confirms group removal, and provides durable cleanup-only recovery without rerendering. Local HTTP failure tests and a live R2 create-only/replay test pass. This is not yet the production bulk controller, unattended reaper, or live GPU shutdown qualification; see the follow-up in `SALAD_BULK_WIRING_AUDIT_2026-09-09.md`.
- [ ] **124 — Keep Novita as a bounded fallback.** Use it only for a verified Salad capacity/runtime failure, with the same input/quality contract and a reconciled prior attempt; never launch both against an uncertain result.
- [ ] **125 — Retain pending and scheduled work in R2.** Preparation, scripts, media, and intermediate results have no expiration before the actual episode release.
- [ ] **126 — Start cleanup from confirmed release.** Reconcile YouTube publication, keep work through release plus fourteen days, and extend the hold if release is delayed or reverted to private.
- [ ] **127 — Keep the final video after cleanup.** Delete only the exact released episode's eligible intermediates; protect other episodes, shared model/worker bundles, reusable library assets, and required compact audit records. Partial (9 September): the [deletion-contract repair](RETENTION_DELETE_CONTRACT_2026-09.md) rejects incomplete object acknowledgements and out-of-run listings, retains known partial counts and final-master evidence, and passes an isolated live R2 retry check; release `139337e` is verified on Vercel, canonical Convex and Trigger `20260909.7`. Successor `134f9e7` rechecks owner/lock/lease/release/connector/run identity before each deletion batch and even empty retries; targeted handlers/worker plus all 629 direct tests, real assembly, build and audits pass. Its Vercel, canonical Convex and Trigger `20260909.8` deployment are verified. Real release-to-cleanup, malformed listing pagination and prepared-media adoption remain open.
- [ ] **128 — Improve healer preflight.** Detect missing credentials, incompatible GPU memory, missing models, broken dependencies, insufficient budget, stale locks, and invalid prompts before dispatching paid work. Partial (9 September): actual GitHub scope verification found an admin-level credential in the worker-pull vault entry. The real Salad proof caller now requires `read:packages` only and rejects broader/unknown permissions or a different registry before R2 job writes/provider creation. Tests and live rejection pass; owner vault correction was requested. No credential rotation or GPU mutation was performed.
- [ ] **129 — Preserve cumulative cost through healing.** Failed and superseded work still counts toward the episode/order budget; rerendering cannot erase previous spend from the budget calculation.
- [ ] **130 — Make healer decisions explainable.** Record root cause, saved artifacts, remaining work, expected incremental cost, chosen retry boundary, and next action without filling the UI with logs.
- [ ] **131 — Test interruption and recovery end to end.** Exercise worker loss, duplicate delivery, expired URLs, R2 outages, missing results, rejected QA, shutdown failure, and provider ambiguity; verify correct routing and bounded spend.
- [ ] **132 — Retire replaced routes after parity proof.** Remove obsolete active dispatch paths and unused wrappers only after real Salad/Novita comparison and consumer tests demonstrate the replacement; retain immutable historical evidence.

### H. Earlier owner requirements that remain active

- [ ] **133 — Preserve owner-only channel and module locks separately.** Only the owner's deliberate unlock in the UI can release a lock. AI, background healers, migrations, retries, and deployment work cannot alter protected code/config through alternate paths. Test both lock scopes and cross-worktree mirrors.
- [ ] **134 — Enforce the exact reusable-asset policy.** Eligible videos may reuse at most 40% of source assets; every third video must use entirely original sourcing. History, crime, heist, and lore story assets are not reusable. Check both planner allocation and actual assembled footage, not just metadata.
- [ ] **135 — Build useful isolated Toolkit containers.** Automatically retain good eligible Stoic/meditation/B-roll assets with identity, rights, quality, source, and usage records. Prove channel isolation, retrieval, effects compatibility, and protection from episode cleanup; reusable assets never excuse unrelated imagery.
- [ ] **136 — Preserve the Lo-Fi thumbnail exception.** Use the video's exact frame at 15 seconds, preserve its scene/daylight/artwork, no headline, and only a 4K symbol in the bottom-right. No generic substitute image, local text overlay, or silent thumbnail-module configuration rewrite. Validate current backfill and future videos.
- [ ] **137 — Preserve native non-Lo-Fi thumbnail typography.** Use the admitted native image-generation pipeline with its current pinned quality contract; do not restore FFmpeg/Satori text compositing, downgraded tiers, or reference-image injection against the owner's decision. Centered subjects remain optional with surrounding composition designed for them.
- [ ] **138 — Preserve the owner's Golden comparison corpus.** Record approved and rejected examples separately: accepted Rome/Inquisition, evil Bill Gates, approved paparazzi, selected ZERO WEAPONS, DEADLY DANCE, and PEACE AWAITS; rejected Empire, Rings of Power, Steve Jobs, Vault, and earlier generic comic/whiteboard examples are not Goldens. Keep later approved revisions distinct from earlier rejected ones. Prove judges select the owner-preferred comparisons without training on their answer labels.
- [ ] **139 — Finish thumbnail backfill and projection consistency.** Library, run details, channel pages, and scheduled cards must show the same accepted current asset with cache invalidation. Local application must not require owner login; external YouTube updates still must satisfy Google's actual channel/feature authorization. Inspect production images, not only database URLs.
- [ ] **140 — Backfill channel-specific identity artwork.** Seaside Ghibli must depict a seaside in the requested illustrated style; Drift & Study remains visibly distinct; Investory must look realistic and finance-specific. Gratitude Springs needs a broad serenity palette, Chalk & Compound must visibly use chalk/tax identity, and Inked Histories must avoid video-game aesthetics. Test both existing channels and automatic future generation.
- [ ] **141 — Preserve purposeful custom-symbol examples.** History uses an opening/closing book and Drawn Past a drawing pen, with equivalent topic-specific symbols elsewhere. Small-size clarity, restrained motion, and reduced-motion variants are acceptance checks; generic rotating ornaments are not a substitute.
- [ ] **142 — Remove specialist navigation clutter completely.** Render fleet, music/lore references, and similar specialist pages are reached through relevant Golden capability cards, not separate sidebar entries or an expanded Tools link dump. Retain useful deep links inside the appropriate module presentation.
- [ ] **143 — Require a creation proof for every new channel.** Each ordinary automated channel creation includes one bounded real test render, channel-identity QA over the whole output, and root-cause repair where needed. Show actual staged creation/render progress. The novel-channel benchmark alone does not satisfy this recurring contract.
- [ ] **144 — Test the real YouTube connection lifecycle.** Cover connect, denied consent, callback, wrong channel, expired access, refresh, reconnect, and upload/scheduled-release handoff. Read-only fixture/screenshot tests are explicitly insufficient evidence for a successful OAuth lifecycle. Remove unnecessary app-level gating without pretending Google permissions can be bypassed.
- [ ] **145 — Make schedule drill-down complete.** Clicking an event reaches its exact channel/episode plan and available script, shot list, image/video renders, SEO, subtitles, narration, and export. The view gains artifacts as they arrive and exposes actual retention state.
- [ ] **146 — Preserve variable-duration formats.** Prove 30-second Shorts, demanding narrated long-form, and 1–8-hour Lo-Fi episodes all use the same declared framework without forcing identical generation costs. Lo-Fi uses the specified two 15-second seamless segments and extended looping with real audio/continuity checks.
  **Measured-duration correction released (9 September):** [Motion Comic evidence](MOTION_COMIC_DURATION_REVIEW_2026-09.md) traces a live 197-second saved estimate to a 200.551-second actual master. Revision `46cf577` probes the exact final file, preserves fractional seconds at the asset boundary, and rejects invalid measurements; recent-video labels use whole elapsed seconds to match native controls. All 639 direct tests, actual assembly, build/typecheck/audits, eleven actual-caller regression cases and retained-byte/native-player proofs passed. The exact Vercel alias and five production playback profiles are verified; CI `34392627171` confirms Convex and Trigger `20260909.24` deployed. This does not repair historical rows, approve their visual quality, or close the other-format duration framework.
  **Cross-module follow-up:** [duration contract audit](DURATION_CONTRACT_AUDIT_2026-09.md) records gate-level counterexamples where a planned runtime passes despite an out-of-bounds delivered master, the expected/measured ABI ambiguity, and removed self-contained duration gates. Preserve independent route-owned expectations while moving metadata to exact measured-master evidence; test the actual compiler and runtime consumers, not a scalar helper alone.
  **Cached-master correction in qualification:** [EDL cache review](EDL_CACHED_MASTER_DURATION_REVIEW_2026-09.md) documents a real cache-hit path returning planned duration without a probe. Cached, fresh and healed final receipts now share a positive/finite measured-duration guard before final-cache/publication. Eleven local assembly suites and retained-byte FFprobe checks pass with no cached render fallback. The operator-gated route is not enabled or production-qualified by this local repair.
- [ ] **147 — Complete the originally requested five review passes.** Record five separate reviews per page/subpanel covering function, hierarchy/layout, visual identity, interaction/motion, and clarity/minimalism; these must include at least three genuine visual iterations per screen as required by item 106, plus the final cross-screen design review. Small typography, useless prose, oversized offline screens, decorative animations, and empty widget areas must not return.
- [ ] **148 — Release accepted stages regularly.** Commit the scoped verified work, push production, verify actual Convex/Trigger deployments and the exact Vercel alias revision, then inspect the affected live UI/API. A successful push or local test is not a production receipt. Keep incomplete or unqualified paid routes disabled until proven.
- [ ] **149 — Finish legacy channel/video retirement deliberately.** Identify exact old pipelines and their retained video IDs; remove or empty obsolete channels and old videos only within the owner's authorized retirement scope. Protect accepted remaining masters, validate replacement routes before thumbnail backfill, and record what was removed and whether it is recoverable. Do not conflate hiding a card, archiving a local record, and deleting a YouTube upload.
- [ ] **150 — Finish the real folder system.** Give channels and episode assets persisted, useful organization with working move/filter/open controls and ownership isolation. Category grouping alone is not the requested folder system; verify current records survive reloads and future automated creation uses the same organization.
- [ ] **151 — Preserve approved provider boundaries.** Use vault credentials and the approved Fal/economical Nano Banana route for relevant artwork. No Google Cloud rendering; Google services remain permitted for YouTube channel management. Do not restore Anthropic dependencies or Qwen text-routing suggestions where the owner selected OpenRouter Gemini Flash. The later explicitly requested Qwen TTS bulk workload is separate. An unavailable Novita benchmark must not halt unrelated goal work or justify silently downgrading quality.

## Completion gate for this phase

This phase is complete only when all 151 items are either passed with linked evidence or covered by an explicitly owner-approved, documented scope exception; every non-thumbnail production module has a current scorecard; the unfamiliar channel succeeds without manual creative substitution; weekly Salad rendering and recovery are proven on real artifacts; full tests and real render validation pass; the exact Convex, Trigger, and Vercel production revisions are verified; and screenshots prove the final UI rather than merely proving it builds. An agent cannot waive unfinished requested work by documenting its own rejection.
