#!/usr/bin/env node

// Permanently retired: channel identity must be created from the canonical
// program brief and original research, never from automatic reference-video
// analysis. This stub deliberately has no provider, network, model, or Convex
// imports and performs no data mutation.
console.error(
  "anchor-lofi-refs is retired: automatic reference-video anchoring is not an allowed channel-creation path.",
);
process.exitCode = 1;
